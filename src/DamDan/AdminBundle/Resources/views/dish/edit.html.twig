{% extends 'base.html.twig' %}

{% block body %}
    {{ breadcrumb({ 'home': 'home', 'admin_index': 'admin', 'admin_dish_index': 'dish', 'admin_dish_show': [dish.title, {id: dish.id}], 'admin_dish_edit': ['edit', {id: dish.id}]}) }}

    <h1>Dish edit</h1>

    {{ form_start(edit_form) }}
    {{ form_widget(edit_form) }}
    <input class="ui inverted button orange" type="submit" value="Edit"/>
    {{ form_end(edit_form) }}

    {{ form_start(delete_form) }}
    <input class="ui inverted button red" type="submit" value="Delete">
    {{ form_end(delete_form) }}

{% endblock %}
